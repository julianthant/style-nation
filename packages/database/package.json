{
  "name": "@style-nation/database",
  "version": "1.0.0",
  "description": "Shared database schema and Prisma client",
  "main": "index.ts",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:prod": "prisma migrate deploy",
    "db:studio": "prisma studio",
    "db:seed": "tsx src/seed.ts",
    "db:reset": "prisma migrate reset",
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5",
    "rimraf": "^6.0.1"
  }
}